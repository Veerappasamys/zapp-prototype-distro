# zapp-prototype-distro

Public PayZapp prototype bootstrap for designers and PMs (Codex, Cursor, etc.).

```bash
curl -fsSL https://raw.githubusercontent.com/Veerappasamys/zapp-prototype-distro/main/bootstrap.mjs | node
```

**v0.2.2** — workspace-local kit cache for sandboxes; no private git fallback.
